// The template's client entry. It exists so Vite's dep scanner can resolve the
// <script> tag in index.html — these tests drive the plugin's dev serving, and no
// browser ever runs this.
export {};
