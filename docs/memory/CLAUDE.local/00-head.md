# rati — the generated always-loaded layer

What a session in this checkout is handed before it opens anything, beside the hand-owned root `CLAUDE.md`. Everything else this repo knows about itself is read on demand: the memory topics this file indexes, the canonical stations under docs/current/, and the effort and finding records under docs/planned/ and docs/backlog/.

## The memory topics

- .memory/conventions.md — what a session must not "clean up": the deliberate shapes in this code, and the habits of landing work here.
