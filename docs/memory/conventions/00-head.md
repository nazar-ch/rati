# Conventions & preferences

The deliberate shapes in rati's code, and the habits of landing work here: what no lint rule, type or test catches, and what a cleanup pass reverses. The public API's naming rule, the CHANGELOG entry and the gate are the root `CLAUDE.md`'s, loaded in every session; a claim lands here when one session paid for it and the next would pay again.
