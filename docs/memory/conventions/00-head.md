# Conventions & preferences

The deliberate shapes in rati's code, and the habits of landing work here: what no lint rule, type or test catches, and what a cleanup pass reverses. The public API's naming rule, the CHANGELOG entry and the gate live in docs/memory/CLAUDE.local/, whose rendering every session loads; a claim lands here when one session paid for it and the next pays again without it.
